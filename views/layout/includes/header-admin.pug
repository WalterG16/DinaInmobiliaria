header(class="bg-sky-400 p-4") 
            div(class="container mx-auto flex justify-between")
                img(src="/img/logo2.png", alt="si" class="h-20")
                a(href="/" class="text-2xl md:flex-auto py-5  px-5 font-bold text-white") DINA Inmobiliaria 
                
                nav(class="my-5 text-sm md:flex md:gap-3 md:items-center font-bold text-white")
                    a(href="/mis-propiedades") Mis Propiedades 

                    form(action="/auth/cerrar-sesion" method="POST")
                        input(type="hidden" name="_csrf" value=csrfToken)
                        input(type="submit" class="bg-sky-600 py-2 px-10 rounded-lg md:flex-auto cursor-pointer" value="Cerrar Sesion") 